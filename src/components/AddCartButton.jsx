import React from "react";
import "bootstrap/dist/css/bootstrap.min.css";

const AddCartButton = ({ btnName }) => {
  return <button className="btn btn-light">{btnName}</button>;
};

export default AddCartButton;
